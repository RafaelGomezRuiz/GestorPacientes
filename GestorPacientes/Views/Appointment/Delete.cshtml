@model PatientManager.Core.Application.ViewModels.Appointment.SaveAppointmentViewModel
@{
    ViewData["Title"] = "Eliminar Usuario";
}

<div class="row">
    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-dark text-light">
                <h4>Esta seguro que desea eliminar la cita?</h4>
            </div>
            <div class="card-body">
                <form method="post" asp-controller="Appointment" asp-action="DeletePost">

                    <input type="hidden" asp-for="Id" />

                    <button type="submit" class="btn btn-primary">Eliminar</button>
                    <a asp-controller="Appointment" asp-action="Index" class="btn btn-warning">Volver atras</a>
                </form>
            </div>
        </div>
    </div>
</div>

